# Environment files — NEVER commit real secrets.
# Only templates (.env.example, .env.docker, .env.test.example) are tracked.
.env
.env.local
.env.production
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.db

# Virtual environments
.venv/
venv/

# Frontend
node_modules/
.next/
frontend/.next/
frontend/node_modules/

# Runtime data — NEVER commit
data/
sessions/
exports/
logs/
events.jsonl
*.log

# Test/cache artifacts
.ruff_cache/
.mypy_cache/
.pytest_cache/
htmlcov/
.coverage
.benchmarks/

# Design drafts and notes — do not carry into new repo
AIV_FRAMEWORK_v*.md
SOUL.md
STATE.md
THIRTEEN_FIXES.md
SKILL.md
reference-patterns-report.md
research_proposal_generation_patterns.md

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
Thumbs.db
.DS_Store
nul

# Misc
.claude-plugin/

# Local tooling state — never commit (agent/session state, not project content)
.ouroboros/
.zcode/

# P1B embedding snapshot — 10MB generated artifact.
# Regenerable via: python -m backend.ranking.generate_embedding_snapshot
# (frozen benchmark fingerprint makes it self-verifying).
# The .fingerprint sidecar + docs/p1b_snapshot/MANIFEST.md are committed
# as provenance; the vectors themselves are regenerated, not stored.
docs/p1b_snapshot/snapshot.json
