# BATCH-53 TASK-02: E2E Smoke Test Fix

**Date:** 2026-05-03
**Status:** ✅ COMPLETE

## Deliverable

### `backend/tests/test_pipeline/test_e2e_mock.py`

Full mock E2E test with 3 test methods:

| Test | Description |
|------|-------------|
| `test_full_pipeline_all_stages` | Executes stages 1-8 in sequence with mock data, verifying each stage produces valid output |
| `test_mock_provider_returns_deterministic` | Confirms MockLLMProvider returns identical results across calls |
| `test_mock_provider_handles_all_stages` | Validates MockLLMProvider returns correct data for each pipeline stage schema |

## Key Design Decisions

### MockLLMProvider
- Schema-based stage detection (inspects `schema["properties"]`) — more reliable than keyword matching in prompts
- Returns deterministic responses for 5 pipeline stages:
  - **Gap analysis** → 2 mock gaps (cross-domain transfer, RAG scalability)
  - **Idea generation** → 2 mock ideas (cross-domain RAG, transformer pruning)
  - **Novelty checking** → score 0.73 with novelty arguments
  - **Feasibility scoring** → score 7.5/10 with timeline and risks
  - **Proposal synthesis** → full proposal with abstract, intro, method, evaluation plan, references

### Stage-by-Stage Mocking
- **Stage 1 (Literature Search):** Mock `SearchService` returns 5 fake papers
- **Stage 2 (Ingestion):** Mock `VectorStore` and `BM25Index`
- **Stage 3 (Gap Analysis):** Real `GapAnalyzer` with `MockLLMProvider`
- **Stage 4 (Idea Generation):** Real `AgentOrchestrator` with `MockLLMProvider`
- **Stage 5 (Novelty):** Real `NoveltyChecker` with mock `VectorStore` (empty results → default high-novelty)
- **Stage 6 (Feasibility):** Real `FeasibilityScorer` with `MockLLMProvider`
- **Stage 7 (Proposal Synthesis):** Real `ProposalSynthesizer` with `MockLLMProvider`
- **Stage 8 (Export):** Real `ExportService` writing to temp directory

### CI Compatibility
- NOT marked `@pytest.mark.slow` or `@pytest.mark.integration`
- No API keys required
- ChromaDB is mocked via `sys.modules` patch
- All optional platform features disabled in test settings

## Test Results

```
backend tests:  1485 passed, 8 skipped  ✅
frontend tests:  337 passed             ✅
No regressions introduced.
```

## Iterations
1. Initial implementation — `get_settings` patch target was wrong (`stages` doesn't import it directly)
2. Fixed patch to `backend.config.get_settings` only
3. Paper count assertion was too strict (2 queries × 5 papers = 10, not 5)
4. MockLLMProvider's keyword-based stage detection misrouted ideation calls to gap response — switched to schema-based detection
5. Added `_idea_response()` method for ideator schema
6. Fixed string literal syntax error (missing quotes)
7. Updated `test_mock_provider_handles_all_stages` to pass actual schemas instead of empty dicts
8. All tests pass ✅
