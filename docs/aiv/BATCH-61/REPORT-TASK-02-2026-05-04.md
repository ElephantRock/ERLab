Task Report
═══════════════════════════════════════════════════════════

Task ID:                 BATCH-61/TASK-02
Report ID:               REPORT-BATCH-61-TASK-02-2026-05-04
Assistant:               Assistant AI
Date:                    2026-05-04
Blueprint Version:       1.0
Dependency:              BATCH-61/TASK-01 (APPROVED — Partial Sign-Off PARTIAL-BATCH-61-TASK-01-2026-05-04)

───────────────────────────────────────────────────────────
TASK DESCRIPTION
───────────────────────────────────────────────────────────
Pipeline Stage Persistence + CLI Resume

Ensure ideas and gaps are saved to DB immediately after their
generation stages complete. Add --resume RUN_ID CLI flag that
skips stages already marked complete in the DB.

───────────────────────────────────────────────────────────
FILES MODIFIED
───────────────────────────────────────────────────────────

1. backend/pipeline/orchestrator.py
   - Added intermediate persist_ideas() call after idea_generation stage (line ~1115)
   - Added skip_stages parameter to run() method signature
   - Added skip_stages check in stage execution loop

2. backend/cli/main.py
   - Added --resume RUN_ID option to generate command
   - Added resume logic: DB lookup, status validation, stages_completed parsing

3. backend/tests/test_pipeline/test_resume.py (created)
   - 9 tests across 3 test classes

───────────────────────────────────────────────────────────
IMPLEMENTATION DETAILS
───────────────────────────────────────────────────────────

Part A — Intermediate Saves (orchestrator.py):

  The orchestrator's run() method already called persist_ideas() after
  feasibility_scoring. The key change is adding a NEW call after
  idea_generation, so ideas are queryable from DB before proposals
  are synthesized (AC-02-01).

  Added block after the idea_generation stage execution:
    if stage.name == "idea_generation":
        self._persistence.persist_ideas(result, db_run_id)
        self._collect_warnings(result)

  The existing feasibility_scoring block remains and updates scores
  once novelty/feasibility reports are available.

Part B — CLI --resume (cli/main.py):

  Added `--resume RUN_ID` as a typer Option on the generate command.

  Resume logic (HB-02 compliant):
    1. Parse RUN_ID as integer (clear error if non-integer)
    2. Look up PipelineRun by primary key via session.get()
    3. Error if not found (AC-02-03)
    4. Error if status is "completed" or "failed"
    5. Parse stages_completed JSON → set of stage names
    6. Pass skip_stages set to orchestrator.run()

Part C — Orchestrator skip_stages (orchestrator.py):

  Added `skip_stages: set[str] | None = None` parameter to run().
  In the stage loop, after optional-stage gating:
    if skip_stages and stage.name in skip_stages:
        logger.info("Skipping completed stage (resume): %s", stage.name)
        continue

  No existing API endpoint contracts changed (HB-03).

───────────────────────────────────────────────────────────
ACCEPTANCE CRITERIA VERIFICATION
───────────────────────────────────────────────────────────

  AC-02-01: Ideas queryable from DB before proposals synthesized
            ✅ persist_ideas() now called after idea_generation stage
            ✅ Verified by TEST-61-02-01

  AC-02-02: --resume RUN_ID skips stages whose current_stage is past
            ✅ skip_stages parameter added to orchestrator.run()
            ✅ CLI parses stages_completed from DB (HB-02)
            ✅ Verified by TEST-61-02-02

  AC-02-03: --resume with invalid RUN_ID produces clear error
            ✅ Non-integer → "not a valid integer ID" error
            ✅ Not found → "RUN_ID not found in database" error
            ✅ Completed/failed → "Cannot resume" error
            ✅ Verified by TestResumeInvalidRunId tests

───────────────────────────────────────────────────────────
HARD BOUNDARIES
───────────────────────────────────────────────────────────

  HB-02: Resume verifies stage completion via pipeline_runs.current_stage
         in DB — ✅ Uses stages_completed JSON from PipelineRun row

  HB-03: No existing API endpoint contract changed — ✅
         Only added optional skip_stages param to orchestrator.run()
         and --resume CLI flag (no API route changes)

───────────────────────────────────────────────────────────
TEST RESULTS
───────────────────────────────────────────────────────────

New tests (test_resume.py):

  TEST-61-02-01: test_ideas_persisted_after_idea_generation      PASS [asyncio]
  TEST-61-02-01: test_persist_ideas_called_after_idea_generation_stage PASS [asyncio]
  TEST-61-02-02: test_skip_stages_skips_completed                PASS [asyncio]
  AC-02-03:      test_invalid_run_id_not_integer                 PASS [sync]
  AC-02-03:      test_invalid_run_id_not_found                   PASS [sync]
  AC-02-03:      test_completed_run_cannot_be_resumed            PASS [sync]

  Trio variants: 3 FAILED — pre-existing DEFER-01 (trio not installed)
  Same issue as TASK-01, tracked in environment setup.

Full suite results:
  python -m pytest backend/tests/test_providers/ backend/tests/test_pipeline/ \
    -q --ignore=backend/tests/test_pipeline/test_e2e_smoke.py

  161 passed, 5 failed (all trio), 8 warnings

  Zero regressions. Baseline was 152 (TASK-01 added 3, TASK-02 added 6 → 161).

───────────────────────────────────────────────────────────
DEFERRED TESTS
───────────────────────────────────────────────────────────

  DEFER-01 (continued): Trio variants fail due to missing trio package.
  Tracked in environment setup — not a code defect.

═══════════════════════════════════════════════════════════
