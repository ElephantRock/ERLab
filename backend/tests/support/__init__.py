"""Test support utilities (synthetic fixtures, deterministic providers).

Used by post-gap downstream proof tests (Commits 4-7). These modules are
test-only and must never be imported by production code.
"""
